[pytest]
pythonpath = .
testpaths = tests
# Live contract tests hit the four real laptops; opt in with `pytest -m live`.
addopts = -m "not live"
markers =
    live: runs against the real four source laptops (opt in with -m live)
