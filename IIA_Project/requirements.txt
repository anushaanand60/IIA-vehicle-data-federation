# Transport layer (mediator + wrappers)
pydantic>=2.6
fastapi>=0.110
uvicorn>=0.29
sqlalchemy>=2.0
httpx>=0.27
# DB drivers — each laptop only needs its own
psycopg2-binary>=2.9   # REG, CAM (PostgreSQL)
pymysql>=1.1           # INS (MySQL)
# THEFT (SQLite) needs no driver
# GUI + tests
streamlit>=1.35
pytest>=8.0
